# 2026-07-14 — F7: Seq::add; ladder budgets; harness staleness fixes

Landed `Seq::add` (the spec `+` on sequences — concatenation), the
highest-value gap from the 63-example Verus-suite scout (it underlies
calc/extensionality proofs and merge sort). Same-slice as always: adapter
lowering (`("Seq","add",2)`), IR `(seqadd expr expr)` + `docs/ir.md`, Lean
parser/printer, prelude model + lemmas, `examples/m2-seqadd`, differential
c127/c128.

- **Model**: `Seq.add = List append`; lemmas `len_add`,
  `index_add_left`, `index_add_right` (boundary split), all proved —
  nothing trusted (docs/trust.md section added).
- **The c127 mismatch and its real cause**: with no `Seq.add` facts,
  `grind` — the ladder's first rung — e-matched the quantified
  per-element `u64` range hypotheses into a deep case split
  (`s.index (t.index j)`-style instantiations) and burned the whole 200k
  heartbeat budget, so the simp rungs that *would* close the goals never
  ran; all three obligations "failed" as 57-second timeouts. Two-part
  fix:
  1. prelude lemmas tagged `@[simp, grind =]` (the Lean-library way, per
     the M4 directive) — `grind` now closes all of c127 in ~1s;
  2. **per-rung heartbeat budgets** in `vrml_ladder`: each deterministic
     rung gets a fresh baseline + 50k cap (`Core.Context.initHeartbeats`/
     `maxHeartbeats` rebound; runtime exceptions caught via
     `tryCatchRuntimeEx` with tactic-state rollback), so a diverging rung
     registers as that rung's failure. `smt` stays last and uncapped.
     Negative guard c128 now rejects in ~2.5s instead of a minute.
- **Harness staleness fixes** (`scripts/run_differential.py`), found the
  hard way when a rebuild didn't surface: (a) it built only `vrml_gen`,
  but generated modules import `Vermilion.Obligations` at judge time —
  now builds `Vermilion` too; (b) the verdict-cache environment stamped
  one `.olean` mtime, which Lake does not reliably rewrite — now stamps a
  sha256 of `lakefile.lean` + all `lean/**/*.lean` sources.
- **Policy set by the user (recorded in
  `.claude/memory/interactive-proofs-first-class.md`)**: for `proof fn`s,
  no automation fine-tuning race for Verus parity — interactive twin
  proofs are first-class ("we can always prove something like this
  directly in Lean"); automation failure must be fast and legible.
  Parity-reporting distinction (automation-pass vs twin-pass) recorded as
  a design TODO in the plan.
- **Tests**: differential corpus now 127 cases — **127/127 verdict
  parity, 61/61 span agreement**, fully cold re-run after the fixes.
  `examples/m2-seqadd`: 3/3 obligations automatic, twin byte-identical.

Scheduled next (plan §Saturation push): prioritized gap-map slices
(type-param uninterp spec fns, `choose`, `to_multiset`+broadcast groups,
`BitNot`, `AssertQuery` nonlinear, statics), the scouting report, an
M1–M4 design-doc audit, and case-studies expansion (Verus repo + online
multi-file projects; merge sort first).

Verification commands (all green):

```console
python3 scripts/run_differential.py   # 127/127, 61/61, cold
./examples/m2-seqadd/run.sh           # 3/3 automatic, twin identical
```
