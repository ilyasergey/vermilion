---
title: Goal-state hover / peek
labels: [feature, vscode-extension]
state: open
github: 4
---

Goal-state hover / peek on any highlighted obligation, without leaving the Rust
file — surface the obligation goal state (as Lean would print it) in a hover or
peek view anchored to the highlighted assertion/contract, so the user does not
have to open the `proofs/` twin to see what must be proven.

*Seed for later expansion — capture the idea, design when scheduled.*
